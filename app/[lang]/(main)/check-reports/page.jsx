import CreateCheckReport from '../../../components/Main/CheckReports/CreateCheckReport';

export default function CheckReportsPage({ params: { lang } }) {
    return <CreateCheckReport lang={lang} />;
}
